# History replaces Lineage in top nav

`/history` = Data Products History. Catalog in
`config/platform/data-products.json`. Method
`data-product.history-review`. CE `dev.signals.dataproduct.updated`.
`/lineage` → `/history`.
