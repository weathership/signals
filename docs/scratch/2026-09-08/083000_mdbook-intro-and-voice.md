# mdbook: federation intro and voice

Visitor landing (`introduction.md`) was still the 2025 classification
product. Rewrote intro, quickstart, overview, engine page, SUMMARY,
and first-seen component/ops pages to match the hub. Book title is
Signals. Docs workflow now fires on `trunk` (it only watched `main`).

Voice: state what the stack is. Classification remains a later
chapter.
