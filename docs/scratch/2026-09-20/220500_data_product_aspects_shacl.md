# Data product Aspects (SHACL Core) on the wire

Protocol `ab515a7`: an Aspect is a NodeShape; ProductSpec composes with
`sh:and`; instances are focus nodes. Agenda attachments are pointers.
`signals.aspects.catalog` inventories shapes and is a product; aspect
ids are not.

Signals seed + `kind=ASPECTS` + warehouse `spec` / `aspect.*` facts
follow. Gaius attachments consume the same proto.
