# Placeholder

This file satisfies Zarf manifest paths until real Knative / YuniKorn / MiNiFi
YAML and charts are vendored. Replace component manifest entries with real
files and remove this placeholder from `zarf.yaml` components that no longer
need it.

Do **not** treat an install of this placeholder as a converged federation stack.
