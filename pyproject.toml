[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "signals"
version = "0.1.0"
description = "Uncertainty-aware column classification for metadata governance"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ortools>=9.15.6755",
    "pyhocon>=0.3.61",
    "scikit-learn>=1.8.0",
    "catboost>=1.2.7",
    "openai>=2.29.0",
    "cython>=3.2.4",
    "ripser>=0.6.14",
    "grpcio>=1.59.0",
    "grpcio-reflection>=1.59.0",
    "requests>=2.31",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
llm = [
    "anthropic>=0.84.0",
]
owl = [
    "owlready2>=0.50",
]
embedding = [
    "sentence-transformers>=3.3.0",
    "numpy>=1.26.0",
]
embedding-catboost = [
    "sentence-transformers>=3.3.0",
    "numpy>=1.26.0",
    "catboost>=1.2.7",
    "scikit-learn>=1.4.0",
]
viz = [
    "pyarrow>=18.0.0",
    "pandas>=2.2.0",
]
sage = [
    "sage-importance>=0.0.6",
    "numpy>=1.26.0",
]
# Headless browser for signals-ui verification (Playwright).
# Prefer devenv `chromium` on PATH; else: just browser-install
browser = [
    "playwright>=1.49.0",
]

[project.scripts]
sigint = "sigint.cli:main"
signals-engine = "signals.engine.server:main"
signals-yk = "signals.cli.yk:main"
signals-sched = "signals.cli.yk:main"
signals-yk-mcp = "signals.mcp.yk:main"
signals-sched-mcp = "signals.mcp.yk:main"

[tool.hatch.build.targets.wheel]
packages = ["src/sigint", "src/signals"]

[dependency-groups]
dev = [
    "anthropic>=0.84.0",
    "behave>=1.3.3",
    "grpcio>=1.59.0",
    "grpcio-tools>=1.59.0",
    "impyla>=0.19.0",
    "kerberos>=1.3.1",  # GSSAPI for Impala HS2 (impyla/puresasl); just backup
    "numpy>=2.4.3",
    "owlready2>=0.50",
    "playwright>=1.49.0",  # headless UI (signals-ui lineup); just browser-install
    "psycopg[binary]>=3.1",
    "pyarrow>=18.0.0",
    "pytest>=8.0",
    "requests>=2.31",
    "sage-importance>=0.0.6",
    "sentence-transformers>=3.3.0",
    "thrift-sasl>=0.4.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
